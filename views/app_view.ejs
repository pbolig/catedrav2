<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/head') %>
    <title><%= title %></title>
</head>
<body>
    <div class="app-container">
        <%- include('partials/header') %>
        
        <aside class="app-sidebar">
            <%- include('partials/sidebar', { unidadesSidebar: unidadesSidebar, materiaActual: materiaActual }) %>
        </aside>

        <main class="app-content-inner">
            <%- include('partials/messages') %>

            <div class="main-content-header">
                 <h1>Unidades de: <%= materiaActual.nombre_materia %></h1>
                 <a href="/app" class="btn-volver">← Volver al Panel</a>
            </div>

            <div class="card nueva-unidad-form">
                <h2>Crear Nueva Unidad</h2>
                <form action="/app/contenido/<%= materiaActual.id %>/unidades" method="POST">
                    <div class="form-group">
                        <label for="nombre_unidad">Nombre de la Unidad</label>
                        <input type="text" id="nombre_unidad" name="nombre_unidad" placeholder="Ej: Introducción a la Programación" required>
                    </div>
                    <button type="submit" class="btn-submit">Crear Unidad</button>
                </form>
            </div>
            
            <div id="contenido-tema">
                <p>Selecciona un tema del menú de la izquierda o crea una nueva unidad para empezar.</p>
            </div>
        </main>
    </div>

    <div id="ia-progress-modal" class="modal-overlay" style="display: none;">
        <div class="modal-content">
            <h2>Generando Contenido con IA</h2>

            <div class="progress-bar-container">
                <div id="ia-progress-bar" class="progress-bar-fill"></div>
            </div>
            <div id="ia-progress-text" class="progress-text">Iniciando...</div>
            <div id="ia-progress-log" class="progress-log">
                </div>
            <div id="ia-modal-footer" class="modal-footer" style="display: none;">
                <button id="btn-cerrar-modal" class="btn-submit">Cerrar</button>
            </div>
        </div>
    </div>

    <script src="/js/app.js"></script>
</body>
</html>